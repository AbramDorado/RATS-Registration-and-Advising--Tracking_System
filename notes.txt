System Outline

Modules
  APIs
  Databases
    Tables
      Columns
  Functionalities
  Frontend
    Components
      Props
    Pages
      Components
      Functionalities
      Variations

Express Server Module
  Functionalities:
    Initializing Express Server {NPM Dependencies: (express), Status: (Done, server.js)}
    Main Function {Status: Done}
    Serve Static Frontend {Status: (Done, server.js > HTML Middleware + Serving Frontend), NPM Dependencies: (connect-history-api-fallback, path)}

Frontend Module
  Frontend Module Features:
    1. Import font families in App.vue
    2. Import background images for Header Component in /public
    3. Import logos for Header Component in /public
    4. Popover Methods in App.vue (hideAllPopovers() and rvClicked())
  Components:
    Header Component {Dependencies: (Frontend Module Feature 1, Frontend Module Feature 2, Frontend Module Feature 3, Frontend Module Feature 4)}
      Props:
        user{role, up_mail, first_name, last_name} Prop
      Header Component Features:
        1. Header Middle Div buttons changes depending on the type of user logged in. {Status: ()}
          For role 'student': Home, Enrollment
          For role 'adviser': Home, Advising
          For role 'ocs': Home, Dashboard
          For role 'admin': Home, Dashboard
        2. Notifications Popover
          To Do: Waiting for Enrollment Module for notifications
        3. Profile Popover
          User can logout
    Footer Component
  Pages:
    Home Page
      Components:
        Header Component
        Footer Component
    Admin Dashboard Page
      Components:
        Header Component
        Footer Component
      Functionalities:
        User Dashboard
          Get All Users
          Register User
          Edit User
          Delete User


Database Module
  Databases:
    db Database {Status: (Done)}
      Tables:
        announcement Table
          Columns:
        enrollment Table
          Columns:
        user Table {Status: (Done)}
          Columns:
            id Column {NPM Dependencies: (uuidv4) }
            role Column
            up_mail Column
            first_name Column
            last_name Column
    sessions Database
      Tables:
        session Table
          Columns:
  Functionalities:
    Utility Function: Opening/Creating Databases {Status: (Done, database/database.js > openOrCreateDB), NPM Dependencies: (sqlite3)}
    Utility Function: Creating Tables {Status: (Done, database/database.js > createTable), NPM Dependencies: (sqlite3)}
    Utility Function: Running SQL Commands {Status: (Done, database/database.js > run), NPM Dependencies: (sqlite3)}
    Utility Function: Get SQL Query {Status: (Done, database/database.js > get), NPM Dependencies: (sqlite3)}
    Utility Function: All SQL Query {Status: (Done, database/database.js > all), NPM Dependencies: (sqlite3)}
    Creating Initial Databases {Status: (Done, server.js > main), NPM Dependencies: (sqlite3)}
    Creating Initial Tables {Status: (Done, database > initial.js), NPM Dependencies: (sqlite3)}
    Creating Initial Rows {Status: (Done, database > initial.js), NPM Dependencies: (sqlite3)}

Authentication Module
  Functionalities:
    1. Passport Setup {Status: (Done), NPM Dependencies: (connect-sqlite3, express-session, passport, passport-google-oidc, sqlite3)}
      Configure GoogleStrategy {Status: (Done)}
      Serialize and Deserialize Functions {Status: (Done)}
    2. Session {Status: (Done), Dependencies : (db Database > user Table, session Database > session Table)}
    3. Auth Router {Status: (To do: Edit User API)}
  APIs:
    Get All Users API (Admin Only) {Status: (Partial), To do: (sorting and search feature in users dashboard; possibly using url parameters)}
    Authorize User API {Status: (Done)}
    Register User API (Admin Only){Status: (Partial), To do: (Allow easy registration of many users; possibly import from csv?, input handling frontend)}
    Login User API {Status: (Done)}
    Logout User API {Status: (Done)}
    Edit User API (Admin Only) {Status: (Not Started)}
    Delete User API (Admin Only) {Status: (Done)}
  Frontend:
    Components:
      Header Component
    Pages:
      Login Page
        Variations:
          ?error=1
          ?loggedOut=true

Announcement Module
  APIs:
    Get All Announcements API
    Add Announcement API
    Edit Announcement API
    Delete Announcement API
  Frontend:
    Components:
      Announcement Component
    Pages:
      Admin Dashboard Page
      Home Page

Enrollment Module

Miscellaneous Module
